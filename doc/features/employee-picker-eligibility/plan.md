Status: complete - 2/2

- [x] Return every confirmed employee from the picker endpoint with a shared primary eligibility result. Add feature tests, implement the server logic, run the test suite, and commit the slice.
- [x] Show eligible employees by default. Add a Show all checkbox that displays blocked employees as disabled rows with the primary reason. Add component tests, implement the user interface and translations, run the test suite, and commit the slice.
