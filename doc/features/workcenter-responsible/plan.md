Status: in progress — 2/3

- [x] Add the `responsible` column, model field, controller validation, and payload. Test create and update.
- [x] Add the Responsible input to the Settings workcenter list. Test that it edits and saves.
- [ ] Show the responsible name in the planning tables, with `-` when blank. Add `responsible` to `PlannedShifts`. Test the service and both pages.
