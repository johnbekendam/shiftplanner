Status: complete — 5/5

- [x] Add the `PlanningTable` component and localized strings.
- [x] Use two tables in `Employees/Form.vue`.
- [x] Use one table in `Personal/Show.vue`; delete `PlannedShiftsList` and its test.
- [x] Update the JS tests.
- [x] Run the JS test suite and `vite build`.
