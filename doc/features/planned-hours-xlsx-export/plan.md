Status: in progress — 1/2

- [x] 1. Add `openspout/openspout`. Make the planned-hours export write `planned-hours.xlsx` with translated headers, the translated sheet name, a date cell (`yyyy-mm-dd`), and an hours cell (`0.00`). Update the feature tests to read the file and check the typed cells.
- [ ] 2. Change the export button label to "Export Excel". Update the Reports JS test.
