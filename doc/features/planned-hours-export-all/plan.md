Status: in progress — 1/2

- [x] 1. Make the export route accept `all=1`. With it, the route ignores From/To, covers every date, and names the file `planned-hours-all.xlsx`. Add feature tests.
- [ ] 2. Add the "Export all to Excel" button in the secondary style, with the `reports.planned_hours.export_all` label. Add a Reports JS test.
