Status: complete — 2/2

- [x] Add a helper that builds an `.ics` string for one shift. Test the fields, the stable UID, and text escaping.
- [x] Add the icon button column to `PlanningTable` for the personal page. Test that the click downloads the file. Send `shift_name`, times, and `responsible` through unchanged.
